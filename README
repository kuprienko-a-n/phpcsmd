DESCRIPTION:

Run phpcs and phpmd test against diff between two branches

OPTIONS:

  -r,  --root       Project root
  -b1, --branch1    First branch for comparison
  -b2, --branch2    Second branch for comparison


USAGE:

1 ./phpcsmd.sh -r=/var/www/magento -b1=BRANCH_ONE -b2=BRANCH_TWO

2 ./phpcsmd.sh --root=/var/www/magento --branch1=BRANCH_ONE --branch2=BRANCH_TWO
